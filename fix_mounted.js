const fs = require('fs'); const path = require('path'); function fixFile(p) { let c = fs.readFileSync(p, 'utf8'); let modified = false; c = c.split('\n').map(line => { if (line.trim().startsWith('setState(') && !c.includes('if (!mounted) return;')) { modified = true; return line.replace('setState(', 'if (!mounted) return; setState('); } return line; }).join('\n'); if (modified) fs.writeFileSync(p, c, 'utf8'); } function walk(dir) { const files = fs.readdirSync(dir); for (const file of files) { const p = path.join(dir, file); if (fs.statSync(p).isDirectory()) walk(p); else if (p.endsWith('.dart')) fixFile(p); } } walk('C:/Users/manch/OneDrive/Documents/Indian-Fitness-app/gymflow/lib/screens');
