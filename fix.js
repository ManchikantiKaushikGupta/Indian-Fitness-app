const fs = require('fs'); const path = require('path'); function walk(dir) { const files = fs.readdirSync(dir); for (const file of files) { const p = path.join(dir, file); if (fs.statSync(p).isDirectory()) walk(p); else if (p.endsWith('.dart')) { let content = fs.readFileSync(p, 'utf8'); let newContent = content.replace(/\\\$/g, '$'); if (content !== newContent) { fs.writeFileSync(p, newContent, 'utf8'); console.log('Fixed', p); } } } } walk('C:/Users/manch/OneDrive/Documents/Indian-Fitness-app/gymflow/lib');
